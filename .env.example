# Core freeze flags (read-only mode for onboarding & dashboards)
CORE_FREEZE=false
NEXT_PUBLIC_CORE_FREEZE=false

# Shop freeze flags (read-only mode for shop/product write operations)
SHOPS_FREEZE=false
NEXT_PUBLIC_SHOPS_FREEZE=false
